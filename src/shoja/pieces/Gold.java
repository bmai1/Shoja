package shoja.pieces;

import shoja.Board;
import shoja.Piece;
import shoja.Position;

public class Gold extends Piece {
    
    public Gold(String side, Position position) {
        super(side, position);
    }
}
